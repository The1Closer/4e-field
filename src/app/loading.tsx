export default function Loading() {
  return (
    <main className="shell-center">
      <section className="panel error-shell">
        <h2 className="error-title">Loading...</h2>
        <p className="hint">Please wait while we sync your field data.</p>
      </section>
    </main>
  );
}
